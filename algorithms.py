
from time import sleep

def orderMethod(method, array, samples):
    samples = int(samples)
    if method == 'Insert':
        return sorted(array[:samples])
    if method == 'Shell':
        return sorted(array[:samples])
    if method == 'Bubble':
        return sorted(array[:samples])
    if method == 'Merge':
        return sorted(array[:samples])
    if method == 'Quicksort':
        return sorted(array[:samples])
    if method == 'Bucket':
        return sorted(array[:samples])
    if method == 'Radix':
        return sorted(array[:samples])
    if method == 'Heap':
        return sorted(array[:samples])
    if method == 'Count':
        return sorted(array[:samples])
    if method == 'Bin':
        return sorted(array[:samples])    
    if method == 'Randomnize':
        return sorted(array[:samples])    
    if method == 'Stooge':
        return sorted(array[:samples])    
    
  